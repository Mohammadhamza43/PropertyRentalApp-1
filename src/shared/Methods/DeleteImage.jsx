import React from 'react'

const DeleteImage = ({image, index , oldImages , setOldImages}) => {
    // const {image , index , oldImages , setOldImages} = props
    // console.log(image);
    // console.log(props.index);
    // console.log(props.oldImages);
    // console.log(props.image);
  return (
    <></>
  )
}

export default DeleteImage